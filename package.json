{
  "name": "classifier",
  "version": "0.0.1",
  "description": "",
  "keywords": "network,neural,bla,bla",
  "license": "",
  "author": "",
  "scripts": {
    "build": "echo \"Error: no test specified\" && exit 1",
    "dev": "echo \"Error: no test specified\" && exit 1",
    "start": "echo \"Error: no test specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
  }
}
